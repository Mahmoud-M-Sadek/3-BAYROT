# 
# مطعم  ع -بيروت | Beirut Restaurant

هذا المشروع هو تطبيق ويب احترافي لمطعم " ع -بيروت"، تم تطويره باستخدام React و TypeScript و Tailwind CSS. يوفر التطبيق واجهة أمامية كاملة للعملاء لعرض قائمة الطعام وتقديم الطلبات، بالإضافة إلى لوحة تحكم شاملة للمسؤولين لإدارة المحتوى والطلبات.

## ✨ الميزات الرئيسية

- **تصميم متجاوب (Responsive):** واجهة مستخدم تتكيف بسلاسة مع جميع أحجام الشاشات، من الهواتف المحمولة إلى أجهزة سطح المكتب.
- **صفحة رئيسية جذابة:** تحتوي على قسم رئيسي (Hero Section) وقائمة بأبرز الأطباق لجذب العملاء.
- **قائمة طعام ديناميكية:** عرض الأصناف ضمن أقسام مع إمكانية البحث والتصفية حسب القسم.
- **سلة تسوق متكاملة:** إضافة المنتجات، تعديل الكمية، وحذفها بسهولة.
- **نظام طلبات:** نموذج لإدخال بيانات العميل وإرسال الطلب.
- **لوحة تحكم للمسؤول:**
  - محمية بكلمة مرور بسيطة.
  - إدارة الطلبات: عرض جميع الطلبات وتغيير حالتها (جديد، جاري التحضير، مكتمل).
  - إدارة قائمة الطعام: إضافة، تعديل، وحذف الأصناف (اسم، وصف، سعر، صورة، قسم).
  - إدارة الأقسام: إضافة، تعديل، وحذف أقسام المنيو.
- **تخزين محلي:** يستخدم `localStorage` لحفظ البيانات (الأصناف، الأقسام، الطلبات، حالة تسجيل الدخول) مما يجعل الحالة ثابتة بين جلسات المتصفح.

## 🛠️ التقنيات المستخدمة

- **الواجهة الأمامية (Frontend):** React.js, TypeScript
- **أداة البناء (Build Tool):** Vite
- **التصميم (Styling):** Tailwind CSS
- **التوجيه (Routing):** React Router DOM
- **إدارة الحالة (State Management):** React Context API
- **الأيقونات (Icons):** Ionicons
- **الخطوط (Fonts):** Google Fonts (Tajawal)

## 🚀 كيفية التشغيل

هذا المشروع عبارة عن تطبيق واجهة أمامية (frontend) فقط ولا يتطلب أي إعداد للخلفية (backend) أو قاعدة بيانات.

### الطريقة الأولى: التشغيل المباشر
1.  **لا توجد خطوات تثبيت:** جميع المكتبات الضرورية (React, Tailwind CSS) يتم تحميلها عبر شبكات توصيل المحتوى (CDN) المضمنة في ملف `index.html`.
2.  **التشغيل:** ببساطة، قم بفتح ملف `index.html` في أي متصفح ويب حديث. سيعمل التطبيق مباشرة.

### الطريقة الثانية: التشغيل عبر خادم محلي (موصى بها)
1.  **تثبيت Node.js:** تأكد من تثبيت Node.js على جهازك من [nodejs.org](https://nodejs.org/).
2.  **تثبيت التبعيات:** قم بتشغيل الأمر التالي في مجلد المشروع:
    ```
    npm install
    ```
3.  **تشغيل الخادم:** قم بتشغيل الأمر التالي:
    ```
    npm run dev
    ```
4.  **فتح التطبيق:** افتح المتصفح وانتقل إلى `http://localhost:5173` (أو الرابط الذي يظهر في الطرفية).



## 📦 رفع المشروع على GitHub

1.  **إنشاء مستودع جديد على GitHub:**
    - انتقل إلى [github.com](https://github.com) وقم بتسجيل الدخول.
    - اضغط على "New repository".
    - أدخل اسم المستودع (مثل: `beirut-restaurant`).
    - اختر النوع (Public أو Private).
    - لا تضع علامة على "Add a README file" أو "Add .gitignore" لأن المشروع يحتوي عليها بالفعل.
    - اضغط على "Create repository".

2.  **تهيئة Git في المشروع:**
    ```
    git init
    git add .
    git commit -m "Initial commit"
    ```

3.  **ربط المستودع المحلي بالمستودع على GitHub:**
    ```
    git remote add origin https://github.com/your-username/beirut-restaurant.git
    git push -u origin main
    ```

4.  **رفع المشروع على GitHub Pages (اختياري):**
    - قم بتثبيت GitHub CLI إذا لم يكن مثبتاً:
      ```
      npm install -g gh
      ```
    - قم بتسجيل الدخول إلى GitHub:
      ```
      gh auth login
      ```
    - قم بتشغيل الأمر التالي لرفع المشروع على GitHub Pages:
      ```
      gh pages deploy --dist dist --branch gh-pages
      ```
    - سيكون رابط الموقع: `https://your-username.github.io/beirut-restaurant/`

## 🔑 الوصول إلى لوحة التحكم

- **الرابط:** انتقل إلى `/#/dashboard` للوصول إلى صفحة تسجيل دخول المسؤول.
- **كلمة المرور:** `admin123`

## 📂 هيكلية المشروع

```
/
├── components/         # مكونات React القابلة لإعادة الاستخدام (Header, Footer)
├── context/            # إدارة الحالة العامة للتطبيق (AppContext)
├── pages/              # مكونات الصفحات الرئيسية (HomePage, MenuPage, DashboardPage, etc.)
├── constants.ts        # البيانات الأولية الثابتة (قائمة الطعام والأقسام)
├── types.ts            # تعريفات TypeScript للأنواع المستخدمة في التطبيق
├── App.tsx             # المكون الرئيسي للتطبيق الذي يحتوي على نظام التوجيه (Routing)
├── index.html          # نقطة الدخول الرئيسية للتطبيق (ملف HTML)
├── index.tsx           # نقطة البداية لتطبيق React
├── metadata.json       # بيانات وصفية للمشروع
├── package.json        # ملف إعدادات npm
├── tsconfig.json       # إعدادات TypeScript
├── vite.config.ts      # إعدادات Vite
└── README.md           # هذا الملف
```
"# 3-BAYROT" 
